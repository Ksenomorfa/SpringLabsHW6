CREATE TABLE user (
  id INTEGER AUTO_INCREMENT,
  firstname varchar (20),
  lastname varchar (30) );

CREATE TABLE country (
  id        INTEGER AUTO_INCREMENT,
  name      VARCHAR(255),
  code_name VARCHAR(255)
)